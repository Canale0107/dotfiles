# Global Instructions

## Language

Respond in Japanese unless the user writes in another language.

## Git

- Commit messages: use Conventional Commits (`type(scope): summary`), e.g. `fix(api): handle empty response body`.
